# Static assets

Drop the Decibel brand mark here as `logo.png` (square, transparent background).
The UI loads `/logo.png` automatically; without it, an inline SVG stand-in is
used. Anything in this folder is served from the site root by Vite/Tauri.
